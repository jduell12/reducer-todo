export const initialState = {
    item: 'Learn about reducers',
    completed: false,
    id: 3892897589
}

export const reducer = (state, action) => {
    switch(action.type){
        default:
            return state;
    }
}